import { PATH_CHANGE_PASSWORD } from 'src/constants/routes'
import { PageMetadata } from 'src/types/general'

const metadata: PageMetadata = {
  public: true,
  title: 'Restablecer contraseña',
  path: PATH_CHANGE_PASSWORD,
}

export default metadata
