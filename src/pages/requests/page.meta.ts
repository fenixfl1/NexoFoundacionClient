import { PageMetadata } from 'src/types/general'
import { PATH_REQUESTS } from 'src/constants/routes'

const metadata: PageMetadata = {
  title: 'Solicitudes',
  path: PATH_REQUESTS,
}

export default metadata
